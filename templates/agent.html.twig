{% extends 'base.html.twig' %}
{% block title %} Agent {% endblock %}

    {% block body %}
	<h3>Ajouter un Agent</h3>
            <div class="row">
                <div class="col s2"></div>
                <div class="col s8">
                    {{ form_start(form)}}
                    {{form_widget(form)}}
                    {{ form_end(form) }}
                </div>
                <div class="col s2"></div>

{% endblock %}