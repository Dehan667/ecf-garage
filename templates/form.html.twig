{% extends 'base.html.twig' %}
{% block title %}Machine{% endblock %}

{% block body %}
<div class="row">
    <div class="col s2"></div>
    <div class="col s8">
        <h3> {{ machine.label }} </h3>
        {{ form_start(form) }}
        {{form_widget(form)}}
        {{ form_end(form) }}
    </div>
    <div class="col s2"></div>
</div>
{% endblock %}